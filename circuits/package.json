{
  "name": "strata-circuits",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Strata withdraw circuit. Logic is Tornado Cash v1, unchanged.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "setup": "node scripts/setup.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "ffjavascript": "^0.3.1",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
